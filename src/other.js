/**
 * Resets the internal data of the application to its initial state
 * @param none
 * @returns {{}}
 */
export function clearV1(){
    let data = {
        userMembers: [],
        channelDetails: [],
    }
    
      return {};
    
}
